---
verdict: needs_changes
provider: codex
model: gpt-6-astra
mode: both
ref: 95f9d1a..HEAD
pass: informed
counts: {blocker: 0, critical: 0, major: 3, minor: 1, nit: 0}
---

# versoes-cifra F0 fix22 both review

Ref `95f9d1a..HEAD`, `src` and `tests`. Codex CLI 0.155.0, model `gpt-6-astra` (self-report `gpt-6`). Local leg was still running when this receipt was written; its findings are added if they are not already these four. Framing Δ: 0 dropped, 4 maintained, 0 emerged.

Reproduced on the plan tree:

1. **major** `src/core/charts.ts:822` — nested tab input: `readMeta` title `Real`, `parse` title `NOTATION`. `writeSongScopedMeta({ title: 'Changed' })` leaves `parse` on `NOTATION`.
2. **major** `src/vue/edit/MetaDialog.vue:205` — untouched `4m26s` is normalized into the patch, so a tempo-only save rewrites duration to `04:26`.
3. **major** `src/core/charts.ts:563` — 8000 alternating `{sos}`/`{sot}` lines throw `RangeError: Maximum call stack size exceeded`. 2000 lines complete.
4. **minor** `src/core/charts.ts:850` — `{sot}` / `{end_of_x_chart}` / `{eot}` / `{title:Real}` parses as `Real` and `readMeta` returns no title.

Do not `done`.
