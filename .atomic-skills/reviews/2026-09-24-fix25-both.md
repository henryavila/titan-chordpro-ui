---
verdict: needs_changes
provider: codex
model: gpt-6-astra
mode: both
ref: b114a12..HEAD
pass: informed
counts: {blocker: 0, critical: 0, major: 1, minor: 0, nit: 0}
---

# versoes-cifra F0 fix25 both review

Ref `b114a12..HEAD`, `src` and `tests`. Codex `gpt-6-astra`, informed pass. Framing Δ: 0 dropped, 1 maintained, 0 emerged.

Reproduced on the plan tree:

1. **major** `src/core/charts.ts:619` — a fence stop is cached as `null`. The full sequence `completa` / `{sos}` / `verso` / `{sos}` / `{sot}` / `{sos}` / `nota` / end / `oferta` / `{eos}` / `{eos}` / end lists `completa`, `verso`, `oferta`. `nota` is inside `verso`. Dropping the first two lines makes `nota` visible.

Do not `done`.
