---
verdict: needs_changes
provider: codex
model: gpt-6-astra
mode: both
ref: 0d27d18..HEAD
pass: informed
counts: {blocker: 0, critical: 0, major: 2, minor: 1, nit: 0}
---

# versoes-cifra F0 fix24 both review

Ref `0d27d18..HEAD`, `src` and `tests`. Codex `gpt-6-astra`, informed pass. Framing Δ: 0 dropped, 2 maintained, 1 emerged.

Reproduced on the plan tree:

1. **major** `src/vue/edit/MetaDialog.vue:381` — `normalizeDurationMmSs('1:04:26')` is `10:42`. `songDurationSec` of the same token is 3866. Enrich would store the wrong length.
2. **major** `tests/core/charts-envelope.test.ts:1946` — `openersThenCharts` hides `nota`. `listCharts` returns only `completa` and `oferta`. The openers never close before `nota`, so the next chart must stay.
3. **minor** `src/core/parse.ts:101` — the tab span for `{sot}` / `{end_of_x_chart}` / `{title:Real}` is lines 0–1, so deleting the tab removes the fence. The text itself excludes the fence.

Do not `done`.
