---
verdict: needs_changes
provider: codex
model: gpt-6-astra
mode: both
ref: 5b2ba91..HEAD
pass: informed
counts: {blocker: 0, critical: 0, major: 1, minor: 0, nit: 0}
---

# versoes-cifra F0 fix27 both review

Ref `5b2ba91..HEAD`, `src` and `tests`. Codex `gpt-6-astra`, informed pass. Framing Δ: 0 dropped, 1 maintained, 0 emerged.

Reproduced on the plan tree:

1. **major** `src/core/charts.ts:709` — one cache key stores a nested boundary and a root fence-stop. `completa` / `{sos}` / `verso` / `{sot}` / `nota` / end / `oferta` / `{eot}` / end lists `completa`, `verso`, `oferta`. Dropping the first two lines shows `nota`. `chartDocument` of `nota` is `{sos}`. `replaceChart` of `verso` deletes `nota`.

Do not `done`.
