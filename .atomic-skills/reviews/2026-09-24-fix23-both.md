---
verdict: needs_changes
provider: codex
model: gpt-6-astra
mode: both
ref: 689a6bf..HEAD
pass: informed
counts: {blocker: 0, critical: 0, major: 3, minor: 0, nit: 0}
---

# versoes-cifra F0 fix23 both review

Ref `689a6bf..HEAD`, `src` and `tests`. Codex `gpt-6-astra`, informed pass. Framing Δ: 0 dropped, 3 maintained, 0 emerged.

Reproduced on the plan tree:

1. **major** `src/vue/edit/MetaDialog.vue:230` — `fieldsToWrite` skips duration whenever it was not touched, so `fillDuration`'s `04:26` never reaches an enveloped chart.
2. **major** `src/core/charts.ts:608` — `settle` recurses on a boundary. 16000 alternating `{sos}`/`{sot}` plus a later chart fence throws `RangeError`. 20 openers return `completa` and `oferta`.
3. **major** `src/core/charts.ts:971` — `{sot}` / `{end_of_x_chart}` / `{title:Real}`: `readMeta` title `Real`, `parse` title empty. A tempo write then shows `Real`. The fence ends the tab, so `parse` must show `Real` before the write.

Do not `done`.
