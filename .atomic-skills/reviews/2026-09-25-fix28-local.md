---
verdict: approve
provider: local
mode: local
ref: a6110ebe5601b8e1777cc5e57011295becf1c4fc
counts: {blocker: 0, critical: 0, major: 0, minor: 0, nit: 0}
---

# Local review — closer cache at HEAD

The nested and root scans share one cached answer, and that answer does not swallow a later chart.

verdict: approve
counts: blocker 0, critical 0, major 0, minor 0, nit 0

## Findings

none

## What was checked

- `closerKey` / `notationBlockCloser` cache hit and `publishedCloser` (`src/core/charts.ts:543-615`): one key is `openAt + kind + chartDepth`, and a fence stop publishes as null.
- `settle` and the depth-0 closer branch (`src/core/charts.ts:628-718`): a closer inside a later chart stores `FENCE_STOP` for the nested frame and its ancestors, same as a root call.
- Parent reuse of a cached child (`src/core/charts.ts:685-695`) and `coversFence` (`src/core/charts.ts:724-727`): a fence stop aborts the parent; a null close does not cover a following `{start_of_x_chart}`.
- `splitCho` fence handling and `spliceInner` (`src/core/charts.ts:266-298`, `src/core/charts.ts:415-417`, `src/core/charts.ts:1361-1387`): `verso` ends at `nota`'s start line, so `replaceChart` does not delete `nota`.
- New cases in `tests/core/charts-envelope.test.ts:2089-2149`.

Product tree at `a6110ebe5601b8e1777cc5e57011295becf1c4fc` matches the reviewed merge `83a906e` for `src/` and `tests/`. Codex informed pass remains `.atomic-skills/reviews/2026-09-24-fix28-codex-pass2.md`.
